framework:
    secret: 'secret'

troopers_cron:
    tasks:
        - command: 'help'
          schedule: '* * * * *'
          arguments:
            command_name: 'cache:clear'
        - command: 'error'
          schedule: '* * * * *'
